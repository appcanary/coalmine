<section>
  <div class="heading">
    <h3>
      <span><%= link_to "dashboard", dashboard_path %></span>
      <% if @server %>
        /
        <span class="name"><%= link_to @server.hostname, server_path(@server) %></span>


        <% unless @app %>
          <% if action_name == 'show' %>
            /
            <span>Apps</span>
          <% end %>

          <% if action_name == 'edit' %>
            /
            <span>Edit</span>
          <% end %>

        <% else %>
          /
          <span><%=link_to "apps", server_path(@server) %></span>
          /
          <span class="name"><%= @app.display_name %></span>
          /
          <span>Packages</span>
        <% end%>
      <% end %>

      <% if @vuln %>
        | vulnerabilities /
        <%=  truncate @vuln.title, length: 50 %>
      <% end %>
    </h3>
  </div>
</section>
