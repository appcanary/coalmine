<dl class="dl-horizontal">
  <dt>name</dt>
  <dd>A name of the app. Can be user specified.</dd>

  <dt>path</dt>
  <dd>The path of the file the app corresponds to.</dd>

  <dt>uuid</dt>
  <dd>A randomly generated identifier. Refer to this field when submitting support requests.</dd>

  <dt>is-vulnerable</dt>
  <dd>A boolean representing whether the monitor contains known vulnerabilities.</dd>

  <dt>vulnerable-versions</dt>
  <dd>An array of ArtifactVersion objects. Only returned in <em>show</em> endpoints, not returned in <em>endpoints</em></dd>
</dl>
