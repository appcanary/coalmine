Hello,

This report summarizes activity between <%= @today %> and <%= @date.to_date.iso8601 %> for <%= Rails.env %>.

Since <%= @date.to_date.iso8601 %>, we have:

---
Users:

<%= @user_ct %> total users
<%= @new_user_ct %> new signups
<%= @active_server_ct %> active servers
<%= @new_server_ct %> new servers
<%= @active_server_bundle_ct %> active server bundles
<%= @monitor_ct %> monitor count
<%= @new_bundle_ct %> new bundles
<%= @total_revenue %> total revenue

---
Email:

<%= @new_vuln_emails_sent %> new vuln emails
<%= @new_patch_emails_sent %> new patch emails

---
System:

<% @new_vuln_ct.each_pair do |platform, ct| %>
  <%= ct %> new <%= platform %> vulns
<% end %>

<%= @new_vulns_detected %> new log_bundle_vulns
<%= @new_patches_detected %> new log_bundle_patches
