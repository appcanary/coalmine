<section id="deploy-puppet" class="<%= hide_deploy_opt_klass(current_user, "puppet") %>">
  <div class="onboard-box">
    <div class="onboard-wrapper">
      <section style="border: none">
        <div class="onboard-title">
          <h3>
            <span class="platform-logo"><%= image_tag "icon-puppet.png", :style => "width: 20px" %> Using puppet</span>
          </h3>
        </div>
      </section>

      <section>
        <div class="onboard-desc">
          <h4>Getting started:</h4>
          <p>Use this command to install the latest compatible version:</p>
            <pre>puppet module install appcanary-appcanary</pre>
          <br>
          <p>If you're using a Puppetfile, be sure to add the following:</p>
              <pre>mod 'computology/packagecloud'</pre>
          <br>
          <h4>Usage</h4>
          <p>Copy the following, replacing the fake paths with correct ones:</p>
<pre class="auth_token">
class { '::appcanary':
  api_key: <%= @agent_token %>
  paths:
    - /path/to/your/Gemfile.lock
    - /a/different/Gemfile.lock
}
</pre>

          <br>
          <p>Bam! You should be good. For slightly more information, <a href="https://forge.puppetlabs.com/appcanary/appcanary">visit the forge page</a>.</p>
        </div>
      </section>

      <section>
        <div class="action-bar">
          <div class="pull-right">
            <%= eui_button "This didn't work. I need support!", href: "mailto:hello@appcanary.com?subject=Problem with puppet", :class => "eui-button-medium-primary feedback-problem", :target => "_blank" %>
            <%= eui_button "Great. I've done all of this!", href: dashboard_path(done: true) %>
          </div>
        </div>
      </section>

    </div>
  </div>
</section>
